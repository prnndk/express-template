import db from "../config/ConnectDb";

async function main() {
  try {
    //enter your seeder function in here
  } catch (err) {
    console.log("Seeder failed:", err);
    process.exit(1);
  }
}

main();
